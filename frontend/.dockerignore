# Dependencies & build output
node_modules
dist
.vite-temp

# Version control & docs (not needed in image)
.git
.gitignore
*.md

# Environment (never bake secrets into image)
.env
.env.*

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Docker (reduces build context)
Dockerfile
.dockerignore
